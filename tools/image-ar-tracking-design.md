# 浏览器端图像 AR 跟踪 · 设计与实现方案

> 目标：在**网页**里做到"对准卡片 → 稳稳跟住 → 鱼不抖不掉"。
> 现在的 pattern（AR.js）能用但抖、纯二维码原型（qr-prototype）思路错在跳过跟踪阶段，
> 本文给出可实现的正确管线。

## 1. 现状与问题

| 方案 | 检测 | 跟踪 | 位姿 | 结果 |
|---|---|---|---|---|
| AR.js pattern（线上主页） | 每帧黑框四边形检测 | **无** | 单应/模板 | 能用，但每帧独立 → 世界坐标一跳一跳 |
| 纯二维码原型（qr-prototype） | 每帧 QR 解码 | **无** | 四点 PnP | 解码失败即丢，10Hz 台阶感 |
| 系统级（ARCore/ARKit/xr-frame） | 离线特征库 | **有（含 IMU 融合）** | 系统级 | 稳 |

**结论：差距不在"检测算法好不好"，而在缺了"跟踪"这一整段。**

## 2. 管线设计

```
相机 640×480 → 灰度化
  │
  ├─【跟踪态】每帧：
  │    ROI 内 KLT 光流跟踪上一帧的内点
  │      → findHomography(RANSAC) 重算单应
  │      → solvePnP(IPPE_SQUARE) 出 6DoF
  │      → One-Euro 滤波 → 写锚点矩阵
  │
  ├─【重检测】满足任一条件时（每 15~30 帧 / 内点<阈值 / 重投影误差超限）：
  │    全图 ORB 检测 + BFMatcher(ratio 0.75) + RANSAC 单应
  │      → 内点 ≥ 20 才算成功，重初始化跟踪
  │
  └─【兜底】连续 2~3 帧重检测失败才判 lost（避免闪烁）
```

### 2.1 检测（Detection）
- 特征：**ORB**（主）/ **AKAZE**（备，抗模糊更强）
- 匹配：BFMatcher + Hamming 距离 + Lowe ratio 0.75 + 交叉验证
- 几何：findHomography(RANSAC, thresh 3px)，内点 ≥ 20
- 卡片特征**离线预算**：ORB 500~1000 个描述子存 JSON（几十 KB）

### 2.2 跟踪（Tracking）—— 稳定性核心
- **KLT**：calcOpticalFlowPyrLK，金字塔 3~4 层，winSize 21×21，200~300 点
- 布点：卡片区域内均匀网格（比角点更抗漂移）+ 上帧强角点
- **ECC**（可选增强）：findTransformECC 对 ROI 做直接对齐，用于 KLT 内点不足时救场
- 单应重算：跟踪点 ↔ 卡片规范坐标 → RANSAC
- ROI：目标周围 1.5 倍区域（省算力 + 抗背景干扰）

### 2.3 位姿（Pose）
- `solvePnP(objPts4, imgPts4, K, dist, rvec, tvec, false, SOLVEPNP_IPPE_SQUARE)`
- 精化：`solvePnPRefineLM`
- 内参：`f = (W/2)/tan(hfov/2)`，hfov 默认 60°，主点=画面中心；需要更准用 `undistort`
- 世界单位：**卡片宽度 = 1**（与现有 fish-motion 的 ±0.7 卡宽/高 0.35~0.9 卡宽一致）

### 2.4 滤波（Filtering）
- **One-Euro filter**（位置 + 四元数各一路）
  - `mincutoff`（默认 1.0，越小越稳）
  - `beta`（默认 0.02，越大越跟手）
- **IMU 融合**：`DeviceMotionEvent` 角速度 → 帧间旋转预测（iOS 需在用户手势里 `requestPermission()`）
- 预测渲染：速度外推 0.5 帧，抵消运动到光子延迟

## 3. 浏览器可用性（已在真实浏览器实测）

```
ORB ✓ AKAZE ✓ BRISK ✓ BFMatcher ✓ findHomography ✓
calcOpticalFlowPyrLK ✓ findTransformECC ✓ matchTemplate ✓
goodFeaturesToTrack ✓ warpPerspective ✓ GaussianBlur ✓
solvePnP ✓ solvePnPRefineLM ✓ Rodrigues ✓ undistort ✓
SOLVEPNP_IPPE_SQUARE=7 ✓ RANSAC=8 ✓ NORM_HAMMING=6 ✓ COLOR_RGBA2GRAY=11 ✓
（缺 estimateAffinePartial2D、ArUco 便捷函数；后者类存在但 JS 构造 API 别扭）
```
来源：`qr-prototype/cv-probe.html` 的实际运行输出。

## 4. 性能预算（手机 640×480）

| 阶段 | 耗时 | 频率 |
|---|---|---|
| KLT 200~300 点 | 3~8 ms | 每帧 |
| RANSAC 单应 | 1~3 ms | 每帧 |
| 位姿 + 滤波 | <1 ms | 每帧 |
| ORB 全图重检测 | 15~40 ms | 每 15~30 帧 / 丢失时 |

→ 稳态可跑 30fps；重检测那一帧可能掉到 20fps，可通过分帧摊开（先检测上半区再下半区）优化。

## 5. 失败判定与状态机

```
LOST ──检测成功(内点≥20)──▶ TRACKING
  ▲                            │
  └──连续 2~3 帧重检测失败──────┘ （内点<50% 或重投影误差 > 5px）
```
- 进入 LOST 后**先不隐藏鱼**：给 1~2 秒宽限（鱼继续按最后一次位姿游），仍失败才走退场动画
- 恢复时**不重置鱼的位置**（避免瞬移），沿用现有 `startEntering/startExiting` 语义

## 6. 调参入口（tracker-lab.html）

实时可调：检测间隔、ratio、RANSAC 阈值/内点下限、KLT winSize/层数/点数、ROI 倍数、
One-Euro `mincutoff`/`beta`、IMU 开关与权重、预测帧数。
可视化：特征点、KLT 轨迹、内点/外点、单应框、重投影误差、每帧耗时、FPS。

## 7. 里程碑与验收

| # | 内容 | 验收 |
|---|---|---|
| M1 | tracker-lab 骨架 + 静态图序列离线验证 | 合成轨迹下，位姿误差曲线可输出 |
| M2 | 检测→KLT→单应→IPPE 最小闭环 | 静态序列 30 帧内不丢；单帧 ≤12ms |
| M3 | One-Euro + 失败判定 + 宽限 | 抖动 RMS 相比裸 PnP 降 ≥50%，延迟增加 ≤1 帧 |
| M4 | IMU 融合（可选） | 快速转动设备时旋转滞后明显减小 |
| M5 | 真机验证 | 报告：丢跟踪次数/分钟、每帧耗时、抖动 RMS、主观跟手度 |

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 水墨卡留白多、特征少 | 先跑**特征密度评估**；不足则卡面加淡纹/印章等纹理，或改用 ArUco 类黑方块 |
| OpenCV.js 体积 10.9MB | 首屏先用 pattern 兜底，加载完再切换跟踪器；或用精简构建 |
| iOS 陀螺仪权限 | 在用户手势（点击开始）里 `requestPermission()`，失败则退化为纯视觉 |
| 跟踪漂移（跟偏了） | 每 15~30 帧强制重检测 + 重投影误差阈值判据 |
| 手机性能差异 | 分辨率自适应（已有）+ 检测分帧摊开 |

## 9. 文件与位置

- 实验与开发：副本仓库 `/Users/mac/aoyuar-qr`（不碰线上）
- 本方案文档：主仓库 `tools/image-ar-tracking-design.md`
- 手机可测的临时页：主仓库 `qr-prototype/`（用完删除）
- 最终合入：主仓库 `js/`（作为 `arjs` 之外的可选跟踪器，按能力自动选择）

## 10. 真机首次验证：卡在"正在打开相机…"的根因（2026-09-21）

手机首次打开 `tracker-prototype/` 时，画面停在"正在打开相机…"，调试行显示
`cv=ok | 卡片=0×0 | 参考=0层`。查下来是**两条各自独立的加载卡点**，都不是跟踪算法本身：

| 根因 | 证据 | 处理 |
|---|---|---|
| 卡片参考图 1.4MB / 2000×2000 | 同一网络下拉这个 PNG 实测 **50.4s**（校验和与本地一致，纯粹是慢）；跟踪器只用得上 480×480（`S.card` 固定画到 480 宽，参考尺度 1.0/0.5/0.32/0.22） | 新增 `assets/patterns/pattern-card-v2-track.png`（480×480，**71KB**）并改跟踪器默认引用；2000×2000 原图保留给 pattern 生成用 |
| 相机启动被 a-scene 的 `loaded` 事件挡住 | 该事件要等 `a-assets` 里两个 GLB（合计 2.1MB）下载完，慢网络下几十秒；期间 `startCamera()` 根本没被调用 | 相机改成 `window.load` 就启动；`tick()` 改为等"锚点已初始化"再跑跟踪。相机画面先出来，鱼等模型 |

附带一点：`solvePnP` / `solvePnPRefineLM` 在退化配置下会抛异常，未接住会打断整个
`requestAnimationFrame` 循环（画面永久卡死）。现在位姿先过 `quadRejectReason`
（有限性/凸性/面积/边长比），不通过就当"本帧没锁住"；真有异常也只记日志并让本帧按丢失处理。

退化压力测试的完整数据与"试过但没采用的手段"见副本仓库
`tracker-lab-results.md` 第九轮（含 `equalizeHist`、KLT 大窗口两组反例）。

## 11. 当前实测（离线合成序列，2026-09-21）

命令：`cd /Users/mac/aoyuar-qr && node tracker-lab-node.js /tmp/card-rgba.bin`（70 帧，静止段 25-55）

| 验收项 | 目标 | 实测 | 结论 |
|---|---|---|---|
| 抖动（静止段 z 标准差） | 比裸 PnP 降 ≥50% | 0.04169 → 0.00861（One-Euro，mincutoff 0.3/beta 0.05） | **降 79%** ✅ |
| 滤波引入延迟 | ≤1 帧 | **+0 帧**（导数互相关口径） | ✅ |
| 稳态帧成本 | <33ms | 跟踪 8.2ms（KLT 3.3 + 单应 4.8）；检测 40ms 每 30 帧一次，摊薄后 ≈1.4ms/帧 | ✅（离线机器） |
| 出位姿 | — | 60/70，判定丢失 1 次 | ✅ |
| 角点误差 | — | 全程 4.62px，静止段 5.63px（卡宽约 250px → ≈2%） | ✅ |
| **浏览器端到端** | 跑通 | 用假摄像头把合成序列喂给**线上那份代码**：全分辨率 43.9-58fps、锁定率 80%、跟踪 4.9ms；`?quality=0.6` 60fps、95%、3.1ms。同一套跑法换输入：模糊 93%、变暗 0.55 70%、噪声 15 32% | ✅ |
| **稳态 30fps（CPU 节流模拟）** | ≥30fps | CDP 把整页拖慢：×1 → 56fps（640×480/8²）；×4 → **33.4fps**（自适应降到 544×408/5²，锁定率 91%）；×8 → 28.3fps（降到底 288×216/5²） | ✅ |
| **真机验证** | 面板读数 | 面板已就绪（`tracker-prototype/` 点面板复制，现含"实测帧间隔"），**待手机数据** | ⏳ |

**现场怎么举卡片（实测）**：卡片在画面里占的宽度决定了能不能稳住。

| 卡片占画面宽 | 出位姿 | 角点误差 中位 |
|---|---|---|
| 35-51% | 59/70 | 2.3 px |
| 29-42% | 61/70 | 3.5 px |
| 23-34% | 54/70 | 8.0 px |
| 17-25% | 48/70 | 2.6 px（但丢帧明显变多） |

说法：**让卡片占到屏幕宽度的三分之一左右**；低于四分之一就会开始丢。

**精度口径提醒**：上面的角点误差跟"参考图的尺度表有没有包住卡片当前大小"强相关。
同一序列只换参考图：1000px 参考（尺度 1000/500/320/220）2.29px、960px 参考 5.12px、
480px 参考 10.93px。浏览器用的是卡片画布 480（尺度 480/240/154/106，覆盖 110-280px），
正好落在上面这张表的可用范围里；曾试过把画布提到 960（尺度最小层 211px），
卡片只有 150px 时反而没有合适尺度，所以**不采用**。

周期重检测间隔取 30 帧（`tracker-lab-results.md` 第十二轮的扫参：干净场景越长越好、运动模糊
越短越好，30 是拐点；相对 45，模糊 13.61→7.99px、噪声 9.74→6.55px、变暗 7.12→5.63px，
干净场景只差 0.44px，检测摊薄 0.93→1.4ms/帧）。

首次捕获是"粗→精"两步：半分辨率全图只负责定位，拿到大致位置后立刻开 ROI 全分辨率重检一次。
改之前卡片只有 ~220px 宽时，粗检会给出带 10° 假旋转、偏 25px 的位姿（然后被当先验跟踪下去）；
改之后帧 0 的四角偏差 ≈1-2px，全程角点误差从 8.14px 降到 3.31px。粗检分辨率固定半分辨率，
不再乘自适应档位（否则档位降到 0.6 时 `ds=0.3`、卡片只剩几十像素，重新捕获会失败）。

**自适应档位怎么定的**：由**实测帧间隔**驱动——`frameMs > 33`（即掉出 30fps）就降档，
跟踪点 8²→5²、处理分辨率 1.0→0.45。原来只看"跟踪+摊薄检测"的成本估值，会漏掉渲染开销
（CPU 节流 ×4 时跟踪报 19ms、真实帧间隔 42ms，档位误以为还有富余）。下限取 0.45/5²：
再往下跟踪点只剩 4²（16 点）或分辨率 224×168（卡片只剩约 56px，参考图最小 106px 对不上）。

**现场光线**（真浏览器实测，见 `tracker-lab-results.md` 第十六轮）：运动模糊不虚（锁定率 93%），
但**变暗 0.55 掉到 70%、噪声 15 只有 32%**，且变暗时位姿是"稳定地偏"（误差 37→58px 平滑漂移），
时序保护救不了。结论：卡片要打光、别在昏暗场地硬拍；确实暗就优先走 AR.js pattern（黑方块对比度天然够）。
预处理试过 preblur 3×3/5×5、CLAHE clip 2/3，**全都得不偿失**，没有采用。

已知短板（都有数据，见副本仓库 `tracker-lab-results.md`）：

| 场景 | 表现 |
|---|---|
| 运动模糊（blur=6 合成） | 35/70、静止段 17.6px：跟踪会断，重锁后精度差。试过 `equalizeHist`（噪声下崩）、KLT 大窗口（正常光照精度掉）、LS 替 RANSAC（角点误差 29-32px），都不划算 → 保持现状 |
| 遮挡 25% | 不再抛异常、不再有自交假位姿；但重锁时检测本身偏 28px（部分遮挡的正常代价） |
| 跟踪帧 7.7ms 的单应开销 | 是鲁棒性买来的：三次尝试用最小二乘替掉它，角点误差都会恶化 5~8 倍（RANSAC 挑最小一致集，LS 平均掉 KLT 漂移） |
